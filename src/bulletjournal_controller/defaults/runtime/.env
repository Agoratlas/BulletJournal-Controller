# Per-instance environment variables for BulletJournal runtime containers.
#
# Add KEY=value entries here. The controller passes this file to Docker with
# --env-file for both environment installs and runtime containers.
#
# Example:
# OPENAI_API_KEY=replace-me
